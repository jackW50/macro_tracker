<h1><%= @food.name %></h1>

<ul>
  <% @food.macronutrients.each do |macronutrient| %>
    <li>
      <%= macronutrient.category ':' macronutrient.grams(@food) ':' macronutrient.calories(@food) %>
    </li>
  <% end %>
</ul>
